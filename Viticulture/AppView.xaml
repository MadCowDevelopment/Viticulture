<controls:MetroWindow x:Class="Viticulture.AppView"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                      xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
                      xmlns:behaviours="http://metro.mahapps.com/winfx/xaml/shared"
                      Title="MainWindow"
                      Height="600"
                      Width="800">

  <i:Interaction.Behaviors>
    <behaviours:BorderlessWindowBehavior ResizeWithGrip="True"/>
  </i:Interaction.Behaviors>
  <!--<controls:MetroWindow.WindowCommands>
    <controls:WindowCommands>
      <Button Content="settings" cal:Message.Attach="OpenSettings"/>
    </controls:WindowCommands>
  </controls:MetroWindow.WindowCommands>-->
  <!--<controls:MetroWindow.Flyouts>
    <controls:FlyoutsControl>
      <controls:Flyout Header="Settings" Background="#9f000000" Position="Right" IsOpen="{Binding IsSettingsFlyoutOpen}">
        <StackPanel>
          <controls:Tile Title="Sample" Count="23">

          </controls:Tile>
        </StackPanel>
      </controls:Flyout>
    </controls:FlyoutsControl>
  </controls:MetroWindow.Flyouts>-->

  <Grid>
    
    <controls:TransitioningContentControl x:Name="MainContent" Transition="Down"/>

  </Grid>

</controls:MetroWindow>
